import { css } from "@/styled-system/css"

export const card = css({
  bg: "surface",
  borderRadius: "lg",
  p: "md",
  boxShadow: "soft",
})

export const titleName = css({
  fontWeight: 600,
  fontSize: "1.4rem",
  mb: "xs",
})
