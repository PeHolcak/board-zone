import { css } from "@/styled-system/css"

export const gameName = css({
  fontSize: "0.875rem",
  fontWeight: 500,
  color: "var(--colors-text)",
})

export const inlineInfoRow = css({
  display: "flex",
  alignItems: "center",
  gap: "0.5rem",
  marginBottom: "0.75rem",
})
